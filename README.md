TrieProject includes of two scripts of python implemented in Python 3.9.

Only the standard libraries is used.

It works with two different command:

**python prep.py**

then

**python query.py "<query-string>"**